{
  "name": "gulp_blank",
  "version": "1.0.0",
  "description": "Blank project on Gulp for build email template, inline style css and send email for tests",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Pelin Oleg",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-email-builder": "^3.0.0",
    "gulp-replace": "^0.5.4"
  }
}
